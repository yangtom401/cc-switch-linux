# cc-switch-web v0.19.1 发布说明

v0.19.1 是 v0.19 功能线的发布质量修复版本，不包含新的运行时功能或数据格式变更。

## 修复

- 将 Skills 更新结果的大小写不敏感排序改为基于键的排序。
- 保持排序结果和业务行为不变，并使源码在当前稳定 Rust 工具链下通过 `cargo clippy -- -D warnings`。

## 兼容性

- 数据库 Schema 版本仍为 `v6`。
- WebDAV v2 兼容版本仍为 `db-v6`。
- 可直接从 v0.19.0 升级，无需迁移或重新配置。
